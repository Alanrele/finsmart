# FinSmart - Railway Deployment

railway_projects:
  - name: finsmart-backend
    build_command: cd backend && npm install
    start_command: cd backend && npm start
    port: 5000
  - name: finsmart-frontend
    build_command: cd frontend && npm install && npm run build
    start_command: cd frontend && npm run preview
    port: 4173
